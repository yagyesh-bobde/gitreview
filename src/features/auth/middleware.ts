// Auth middleware is configured via the `authorized` callback in auth.config.ts
// and exported from the root middleware.ts as `export { auth as middleware }`.
//
// This file exists as a reference point in the auth feature module.
// The actual middleware logic lives in auth.config.ts > callbacks.authorized.
